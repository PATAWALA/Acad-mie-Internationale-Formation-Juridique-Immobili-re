// app/dashboard/etudiant/page.tsx
// Cette page est rendue inutile par le DashboardLayout qui gère tout en interne.
// Redirige simplement vers le layout.

import { redirect } from 'next/navigation';

export default function StudentPage() {
  // Le DashboardLayout dans layout.tsx gère tout
  return null;
}